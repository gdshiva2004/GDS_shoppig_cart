[project]
name = "shopping-cart-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "NallavalliDharma", email = "nallavallidharma@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "loguru>=0.7.3",
    "pymongo>=4.18.1",
]

[project.scripts]
shopping-cart-project = "shopping_cart_project:main"

[build-system]
requires = ["uv_build>=0.12.18,<0.13.0"]
build-backend = "uv_build"
